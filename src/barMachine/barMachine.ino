#include "common.h"
#include "functions.h"

void setup()
{
  stp.setSpeed(10);
  pinMode(PIN_RELAY, OUTPUT);
}

void loop()
{
  /*
  changeValve(true, 500);
  pumpON();
  delay(500);
  pumpOFF();
  delay(500);
  */
}
